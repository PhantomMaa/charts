apiVersion: v1
kind: Service
metadata:
  name: {{ include "prefect.fullname" . }}
  labels:
    {{- include "prefect.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prefect.selectorLabels" . | nindent 4 }}
