apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "prefect.fullname" . }}-server
  labels:
    {{- include "prefect.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.server.replicaCount }}
  selector:
    matchLabels:
      {{- include "prefect.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: server
  template:
    metadata:
      labels:
        {{- include "prefect.labels" . | nindent 8 }}
        app.kubernetes.io/component: server
    spec:
      containers:
        - name: server
          image: "{{ .Values.server.image.repository }}:{{ .Values.server.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.server.image.pullPolicy }}
          command: ["prefect", "server", "start"]
          ports:
            - name: http
              containerPort: 4200
              protocol: TCP
          resources:
            {{- toYaml .Values.server.resources | nindent 12 }}
          env:
            {{- range $key, $value := .Values.server.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "prefect.fullname" . }}-worker
  labels:
    {{- include "prefect.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.worker.replicaCount }}
  selector:
    matchLabels:
      {{- include "prefect.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: worker
  template:
    metadata:
      labels:
        {{- include "prefect.labels" . | nindent 8 }}
        app.kubernetes.io/component: worker
    spec:
      containers:
        - name: worker
          image: "{{ .Values.worker.image.repository }}:{{ .Values.worker.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.worker.image.pullPolicy }}
          command: ["prefect", "worker", "start", "--pool", "{{ .Values.worker.pool_name }}"]
          resources:
            {{- toYaml .Values.worker.resources | nindent 12 }}
          env:
            {{- range $key, $value := .Values.worker.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}