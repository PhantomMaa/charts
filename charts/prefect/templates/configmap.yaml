apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "prefect.fullname" . }}
  labels:
    {{- include "prefect.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{- .Values.config | toYaml | nindent 4 }}
